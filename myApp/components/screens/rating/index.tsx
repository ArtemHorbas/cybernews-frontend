import { FC } from 'react'
import { MainLayout } from '@/layouts/main'

export const RatingScreen: FC = () => {
	return (
		<MainLayout title={'Rating Page'} mainHeader ratingHeader>
			RATING
		</MainLayout>
	)
}
